[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "pickcardu-contracts"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["pickcardu-rag-api==0.1.0", "PyYAML==6.0.2"]
